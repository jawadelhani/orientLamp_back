# Example .env for orientLamp_back (DO NOT store real secrets here)
# Copy this to .env and fill values for local development.

# MySQL
MYSQL_ROOT_PASSWORD=changeme
MYSQL_DATABASE=orientlamp_db

# SMTP (Gmail or transactional SMTP)
SPRING_MAIL_USERNAME=your-email@example.com
SPRING_MAIL_PASSWORD=your-smtp-password

# JWT and app secrets
JWT_SECRET=replace_with_a_secure_random_value

# Email
EMAIL_VERIFICATION_FROM=noreply@your-domain.com
